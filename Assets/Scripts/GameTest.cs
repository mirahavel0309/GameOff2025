// Attach to a temporary test GameObject
using UnityEngine;

public class GameTest : MonoBehaviour
{
    [SerializeField] private PlayerDeck playerDeck;

    void Update()
    {
        //if (Input.GetKeyDown(KeyCode.Space))
        //{
        //    playerDeck.Draw();
        //}
    }
}
